import HomePage from 'commons/pageObjects/home';
import SearchPage from 'commons/pageObjects/search';

export const CommonPages = {
    desktop: {
        HomePage: HomePage,
        SearchPage: SearchPage,
    },
    mobile: {
        HomePage: HomePage,
        SearchPage: SearchPage,
    }
}